# 🎮 Blueprint: CustomMovementMacroLibrary

**[Classe Pai / Parent Class: `Object`]**

## ⚙️ 1. Variáveis Declaradas (Variables)
*Nenhuma variável explícita declarada no painel de controle.*

## ⚙️ 2. Grafos de Eventos e Lógica (Graphs)
### 📌 Grafo: `IsCustomMovement`
- 🔀 Contém `1` nós de decisão (`Branch/If`).

**Funções e Métodos Chamados:**
- 🛠️ `IsCustomMovement()`

## ❓ Perguntas que este documento responde
- Qual é a classe pai do Blueprint `CustomMovementMacroLibrary`?
- Quais funções e eventos são chamados no grafo do `CustomMovementMacroLibrary`?