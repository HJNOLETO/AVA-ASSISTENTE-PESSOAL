# 🎮 Blueprint: Blueprint_WallSconce

**[Classe Pai / Parent Class: `Actor`]**

## ⚙️ 1. Variáveis Declaradas (Variables)
| Nome da Variável | Tipo de Dado |
| :--- | :--- |
| `Brightness` | `real (double)` |
| `Color` | `struct (LinearColor)` |
| `Inner Cone Angle` | `real (double)` |
| `Outer Cone Angle` | `real (double)` |

## ⚙️ 2. Grafos de Eventos e Lógica (Graphs)
### 📌 Grafo: `EventGraph`

### 📌 Grafo: `UserConstructionScript`

**Funções e Métodos Chamados:**
- 🛠️ `SetIntensity()`
- 🛠️ `SetLightColor()`
- 🛠️ `SetInnerConeAngle()`
- 🛠️ `SetOuterConeAngle()`

**Variáveis Manipuladas:**
- `Get Brightness`
- `Get Color`
- `Get Inner Cone Angle`
- `Get Outer Cone Angle`
- `Get PointLight2`

### 📌 Grafo: `ExecuteUbergraph_Blueprint_WallSconce`

### 📌 Grafo: `UserConstructionScript_MERGED`

**Funções e Métodos Chamados:**
- 🛠️ `SetIntensity()`
- 🛠️ `SetLightColor()`
- 🛠️ `SetInnerConeAngle()`
- 🛠️ `SetOuterConeAngle()`

**Variáveis Manipuladas:**
- `Get Brightness`
- `Get Color`
- `Get Inner Cone Angle`
- `Get Outer Cone Angle`
- `Get PointLight2`

## ❓ Perguntas que este documento responde
- Qual é a classe pai do Blueprint `Blueprint_WallSconce`?
- Quais variáveis estão disponíveis no Blueprint `Blueprint_WallSconce`?
- Quais funções e eventos são chamados no grafo do `Blueprint_WallSconce`?