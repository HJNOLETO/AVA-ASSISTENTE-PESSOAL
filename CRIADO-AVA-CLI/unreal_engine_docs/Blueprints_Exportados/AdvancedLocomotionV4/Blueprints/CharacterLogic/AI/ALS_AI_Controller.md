# 🎮 Blueprint: ALS_AI_Controller

**[Classe Pai / Parent Class: `AIController`]**

## ⚙️ 1. Variáveis Declaradas (Variables)
*Nenhuma variável explícita declarada no painel de controle.*

## ⚙️ 2. Grafos de Eventos e Lógica (Graphs)
### 📌 Grafo: `EventGraph`

**Eventos de Entrada (Events):**
- 🟢 `ReceivePossess`

**Funções e Métodos Chamados:**
- 🛠️ `RunBehaviorTree()`
- 🛠️ `CharBeginPlay()`

### 📌 Grafo: `UserConstructionScript`

### 📌 Grafo: `ExecuteUbergraph_ALS_AI_Controller`

**Eventos de Entrada (Events):**
- 🟢 `ReceivePossess`

**Funções e Métodos Chamados:**
- 🛠️ `RunBehaviorTree()`
- 🛠️ `CharBeginPlay()`

### 📌 Grafo: `ReceivePossess`

**Funções e Métodos Chamados:**
- 🛠️ `ExecuteUbergraph_ALS_AI_Controller()`

### 📌 Grafo: `UserConstructionScript_MERGED`

## ❓ Perguntas que este documento responde
- Qual é a classe pai do Blueprint `ALS_AI_Controller`?
- Quais funções e eventos são chamados no grafo do `ALS_AI_Controller`?