# 🎮 Blueprint: GroundedEntryState_AnimNotify

**[Classe Pai / Parent Class: `AnimNotify`]**

## ⚙️ 1. Variáveis Declaradas (Variables)
| Nome da Variável | Tipo de Dado |
| :--- | :--- |
| `Grounded Entry State` | `byte (GroundedEntryState)` |

## ⚙️ 2. Grafos de Eventos e Lógica (Graphs)
### 📌 Grafo: `Received_Notify`

**Funções e Métodos Chamados:**
- 🛠️ `GetAnimInstance()`
- 🛠️ `BPI_SetGroundedEntryState()`

**Variáveis Manipuladas:**
- `Get Grounded Entry State`

### 📌 Grafo: `GetNotifyName`

**Variáveis Manipuladas:**
- `Get Grounded Entry State`

### 📌 Grafo: `Received_Notify_MERGED`

**Funções e Métodos Chamados:**
- 🛠️ `GetAnimInstance()`
- 🛠️ `BPI_SetGroundedEntryState()`

**Variáveis Manipuladas:**
- `Get Grounded Entry State`

### 📌 Grafo: `GetNotifyName_MERGED`

**Funções e Métodos Chamados:**
- 🛠️ `GetEnumeratorUserFriendlyName()`

**Variáveis Manipuladas:**
- `Get Grounded Entry State`

## ❓ Perguntas que este documento responde
- Qual é a classe pai do Blueprint `GroundedEntryState_AnimNotify`?
- Quais variáveis estão disponíveis no Blueprint `GroundedEntryState_AnimNotify`?
- Quais funções e eventos são chamados no grafo do `GroundedEntryState_AnimNotify`?