# 🎮 Blueprint: OverlayOverride_NotifyState

**[Classe Pai / Parent Class: `AnimNotifyState`]**

## ⚙️ 1. Variáveis Declaradas (Variables)
| Nome da Variável | Tipo de Dado |
| :--- | :--- |
| `OverlayOverrideState` | `int` |

## ⚙️ 2. Grafos de Eventos e Lógica (Graphs)
### 📌 Grafo: `Received_NotifyBegin`

**Funções e Métodos Chamados:**
- 🛠️ `GetAnimInstance()`
- 🛠️ `BPI_SetOverlayOverrideState()`

**Variáveis Manipuladas:**
- `Get OverlayOverrideState`

### 📌 Grafo: `Received_NotifyEnd`

**Funções e Métodos Chamados:**
- 🛠️ `GetAnimInstance()`
- 🛠️ `BPI_SetOverlayOverrideState()`

### 📌 Grafo: `GetNotifyName`

**Funções e Métodos Chamados:**
- 🛠️ `Conv_IntToString()`

**Variáveis Manipuladas:**
- `Get OverlayOverrideState`

### 📌 Grafo: `Received_NotifyBegin_MERGED`

**Funções e Métodos Chamados:**
- 🛠️ `GetAnimInstance()`
- 🛠️ `BPI_SetOverlayOverrideState()`

**Variáveis Manipuladas:**
- `Get OverlayOverrideState`

### 📌 Grafo: `Received_NotifyEnd_MERGED`

**Funções e Métodos Chamados:**
- 🛠️ `GetAnimInstance()`
- 🛠️ `BPI_SetOverlayOverrideState()`

### 📌 Grafo: `GetNotifyName_MERGED`

**Funções e Métodos Chamados:**
- 🛠️ `Conv_IntToString()`

**Variáveis Manipuladas:**
- `Get OverlayOverrideState`

## ❓ Perguntas que este documento responde
- Qual é a classe pai do Blueprint `OverlayOverride_NotifyState`?
- Quais variáveis estão disponíveis no Blueprint `OverlayOverride_NotifyState`?
- Quais funções e eventos são chamados no grafo do `OverlayOverride_NotifyState`?