# 🎮 Blueprint: OverlayStateButton

**[Classe Pai / Parent Class: `UserWidget`]**

## ⚙️ 1. Variáveis Declaradas (Variables)
| Nome da Variável | Tipo de Dado |
| :--- | :--- |
| `BackgroundSelectedColor` | `struct (LinearColor)` |
| `BackgroundUnSelectedColor` | `struct (LinearColor)` |
| `TextSelectedColor` | `struct (SlateColor)` |
| `TextUnSelectedColor` | `struct (SlateColor)` |

## ⚙️ 2. Grafos de Eventos e Lógica (Graphs)
### 📌 Grafo: `SetVisualParameters`
- 🔀 Contém `1` nós de decisão (`Branch/If`).

**Funções e Métodos Chamados:**
- 🛠️ `SetColorAndOpacity()`

**Variáveis Manipuladas:**
- `Get Background`
- `Get BackgroundSelectedColor`
- `Get BackgroundUnSelectedColor`
- `Get Text`
- `Get TextSelectedColor`
- `Get TextUnSelectedColor`

### 📌 Grafo: `EventGraph`

### 📌 Grafo: `ExecuteUbergraph_OverlayStateButton`

### 📌 Grafo: `SetVisualParameters_MERGED`
- 🔀 Contém `1` nós de decisão (`Branch/If`).

**Funções e Métodos Chamados:**
- 🛠️ `SetColorAndOpacity()`

**Variáveis Manipuladas:**
- `Get Background`
- `Get BackgroundSelectedColor`
- `Get BackgroundUnSelectedColor`
- `Get Text`
- `Get TextSelectedColor`
- `Get TextUnSelectedColor`

## ❓ Perguntas que este documento responde
- Qual é a classe pai do Blueprint `OverlayStateButton`?
- Quais variáveis estão disponíveis no Blueprint `OverlayStateButton`?
- Quais funções e eventos são chamados no grafo do `OverlayStateButton`?