# 🎮 Blueprint: BP_Functions

**[Classe Pai / Parent Class: `BlueprintFunctionLibrary`]**

## ⚙️ 1. Variáveis Declaradas (Variables)
*Nenhuma variável explícita declarada no painel de controle.*

## ⚙️ 2. Grafos de Eventos e Lógica (Graphs)
### 📌 Grafo: `CenterMousePosition`

**Funções e Métodos Chamados:**
- 🛠️ `GetViewportSize()`
- 🛠️ `BreakVector2D()`
- 🛠️ `Divide_DoubleDouble()`
- 🛠️ `GetPlayerController()`
- 🛠️ `SetMouseLocation()`
- 🛠️ `FTrunc()`

### 📌 Grafo: `GetPhysicalMaterial`

**Variáveis Manipuladas:**
- `Get SurfaceType`

### 📌 Grafo: `GetWeaponSystem`

**Funções e Métodos Chamados:**
- 🛠️ `GetComponentByClass()`

### 📌 Grafo: `CenterMousePosition_MERGED`

**Funções e Métodos Chamados:**
- 🛠️ `GetViewportSize()`
- 🛠️ `BreakVector2D()`
- 🛠️ `Divide_DoubleDouble()`
- 🛠️ `GetPlayerController()`
- 🛠️ `SetMouseLocation()`
- 🛠️ `FTrunc()`

### 📌 Grafo: `GetPhysicalMaterial_MERGED`

**Variáveis Manipuladas:**
- `Get SurfaceType`

### 📌 Grafo: `GetWeaponSystem_MERGED`

**Funções e Métodos Chamados:**
- 🛠️ `GetComponentByClass()`

## ❓ Perguntas que este documento responde
- Qual é a classe pai do Blueprint `BP_Functions`?
- Quais funções e eventos são chamados no grafo do `BP_Functions`?